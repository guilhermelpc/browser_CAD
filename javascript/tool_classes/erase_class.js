import { GlobalElems, GlobalState } from '../global_state.js';
import { parseCoords } from '../command_exec.js';
import { updateTimelineCLI, resetCliInput } from '../cli_utils.js';

export class Erase {
    constructor() {
    }

    handleInput(input) {
    }

    updateCommand(cursorPos = null) {
    }

    consolidateCommand() {
    }

    saveState() {
    }

    restoreState(state) {
    }

    cancel() {
    }

}